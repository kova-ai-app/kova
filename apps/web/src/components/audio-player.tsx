'use client'

export function AudioPlayer(_props: {
  src: string
  onTimeUpdate: (t: number) => void
  seekTo: number | null
  onSeeked: () => void
}) {
  return <div>Audio player placeholder — replaced in Task 8</div>
}
